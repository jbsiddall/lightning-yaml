# Lightning YAML — CONFORMANCE benchmark suite (yaml-test-suite pass rate).
#
# Append-only, multi-document YAML (same model as speed.yaml): each '---'
# document is one full suite RUN; CI appends, never rewrites.
#
# score is the pass rate as a percent; higher is better.
#
# schema_version pins the doc shape for validation (bench/schemas.ts). Competitor
# rows carry the `version` they were scored at; the self (lightning-yaml) row is
# pinned by the run's own `source` commit instead. Emitted runs additionally
# record `generated_at` (a full ISO-8601 timestamp beside the date-only
# `generated`) — omitted here so no fabricated time ships with the sample.
---
suite: conformance
scope: competition
suite_total: 373
unit: "%"
higher_is_better: true
schema_version: 1
generated: "2026-07-13"
source: "README.md@6b0c56e"
results:
  - { id: lightning-yaml, label: Lightning YAML, self: true, passed: 364, total: 373, score: 97.6, negative_passed: 91, negative_total: 91 }
  - { id: yaml,           label: yaml (eemeli), version: "2.9.0", passed: 362, total: 373, score: 97.1 }
  - { id: js-yaml,        label: js-yaml, version: "5.2.1",   passed: 354, total: 373, score: 94.9 }
